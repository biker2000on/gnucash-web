---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified: [src/app/api/accounts/route.ts, src/app/api/transactions/route.ts, src/lib/format.ts]
autonomous: true

must_haves:
  truths:
    - "Accounts and Transactions APIs return data from Prisma"
    - "BigInt fields are correctly serialized in JSON responses"
    - "API endpoints maintain existing response structures"
  artifacts:
    - path: "src/app/api/accounts/route.ts"
      provides: "Accounts list via Prisma"
    - path: "src/app/api/transactions/route.ts"
      provides: "Transactions list via Prisma"
  key_links:
    - from: "src/app/api/accounts/route.ts"
      to: "src/lib/prisma.ts"
      via: "import"
---

<objective>
Migrate the primary API routes (Accounts and Transactions) from raw SQL to Prisma and handle BigInt serialization.

Purpose: Validate the Prisma implementation on core application features and fix serialization issues.
Output: Working Prisma-backed core API endpoints.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BigInt Serialization Fix</name>
  <files>src/lib/format.ts</files>
  <action>
    Add a global BigInt serialization fix (BigInt.prototype.toJSON) or implement a utility function 
    to transform Prisma results before sending as JSON.
    Research recommended adding to global layout or utility.
  </action>
  <verify>API responses no longer throw on BigInt fields</verify>
  <done>BigInt serialization is handled project-wide or via utility.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Accounts and Transactions API</name>
  <files>src/app/api/accounts/route.ts, src/app/api/transactions/route.ts</files>
  <action>
    1. Update src/app/api/accounts/route.ts to use prisma.account.findMany().
    2. Update src/app/api/transactions/route.ts to use prisma.transaction.findMany().
    3. Ensure existing sorting and filtering logic is preserved.
    4. Replace raw SQL and pg pool usage.
  </action>
  <verify>Manual check or API tests pass for these routes</verify>
  <done>Core API routes are running on Prisma.</done>
</task>

</tasks>

<verification>
Verify API endpoints /api/accounts and /api/transactions return correct JSON via browser or curl.
</verification>

<success_criteria>
Core APIs are migrated to Prisma and successfully return data.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
