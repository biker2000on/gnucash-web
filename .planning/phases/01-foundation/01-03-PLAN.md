---
phase: 01-foundation
plan: 03
type: tdd
wave: 2
depends_on: [01-01, 01-02]
files_modified: [src/lib/prisma.ts, src/lib/__tests__/numeric.test.ts]
autonomous: true

must_haves:
  truths:
    - "Prisma client automatically calculates decimals from fraction fields"
    - "Conversion logic handles negative values and zero denominators"
    - "Tests cover standard GnuCash fraction patterns (100, 1000 denominators)"
  artifacts:
    - path: "src/lib/prisma.ts"
      provides: "Prisma extension for numeric conversion"
  key_links:
    - from: "src/lib/prisma.ts"
      to: "toDecimal logic"
      via: "extension compute function"
---

<objective>
Implement and verify the Prisma extension logic for fraction-to-decimal conversion using TDD.

Purpose: Ensure financial data is accurately converted from GnuCash's internal format to decimals.
Output: Robust, tested Prisma extension.
</objective>

<feature>
  <name>GnuCash Fraction to Decimal Conversion</name>
  <files>src/lib/prisma.ts, src/lib/__tests__/numeric.test.ts</files>
  <behavior>
    The Prisma extension should add a 'value_decimal' field to models with 'value_num' and 'value_denom'.
    Cases:
    - 100/100 -> "1"
    - 150/100 -> "1.50"
    - -50/100 -> "-0.50"
    - 0/100 -> "0"
    - 12345/1000 -> "12.345"
  </behavior>
  <implementation>
    Use Prisma Client Extension ($extends) with a 'result' block.
    Integrate or adapt the existing toDecimal logic from src/lib/db.ts.
  </implementation>
</feature>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>RED: Add failing tests for numeric conversion</name>
  <files>src/lib/__tests__/numeric.test.ts</files>
  <action>
    Create src/lib/__tests__/numeric.test.ts with test cases for the expected toDecimal behavior, 
    initially testing the standalone function if possible, or the prisma extension if mocks are ready.
  </action>
  <verify>npm test src/lib/__tests__/numeric.test.ts fails</verify>
  <done>Failing tests represent the requirements.</done>
</task>

<task type="auto">
  <name>GREEN: Implement conversion in Prisma extension</name>
  <files>src/lib/prisma.ts</files>
  <action>
    Implement the numeric conversion logic in the Prisma singleton's $extends block.
    Ensure 'split' and other relevant models are covered.
  </action>
  <verify>npm test src/lib/__tests__/numeric.test.ts passes</verify>
  <done>Conversion logic works correctly according to tests.</done>
</task>

</tasks>

<verification>
Run all tests to ensure no regressions in testing infrastructure.
</verification>

<success_criteria>
Prisma client accurately converts GnuCash fractions to decimals.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
