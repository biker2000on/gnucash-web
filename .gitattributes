# Shell scripts must keep LF endings — the Docker image executes them
# directly (a CRLF shebang breaks ./docker-entrypoint.sh on Windows checkouts).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
