# A list of bugs to be fixed

* clearing filters on general ledger doesn't reload all transactions
* transaction modal doesn't always show up int he middle of current viewport, but sometimes autoscrolls to the last time it was opened. 
* reconcile feature needs work. Sums don't total for selected items, etc.